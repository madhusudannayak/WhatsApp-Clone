import 'package:flutter/material.dart';

class StatusCreen extends StatelessWidget {
  const StatusCreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}